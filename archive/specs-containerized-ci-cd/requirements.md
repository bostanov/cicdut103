# Спецификация требований - Контейнеризованная CI/CD система для 1С

## Введение

Данная спецификация описывает требования к созданию полной контейнеризованной CI/CD системы для проекта 1С УТ 10.3, включающей объединенный контейнер GitSync+PreCommit1C, GitLab, Redmine, PostgreSQL и SonarQube. Система решает существующие проблемы с Windows службами и обеспечивает правильную интеграцию всех компонентов в едином контейнерном окружении.

## Анализ текущих проблем

### Критические проблемы
1. **GitSync Service не запускается** - ошибка Windows Service (код 5)
2. **OneScript не установлен** - блокирует установку PreCommit1C
3. **Неправильная интеграция контейнеров** - существующие контейнеры работают изолированно
4. **Отсутствие координации между GitSync и PreCommit1C**
5. **Сложность диагностики и поддержки Windows служб**

### Преимущества полной контейнеризации
- Изоляция всех компонентов с правильной интеграцией
- Единое окружение для всех сервисов CI/CD
- Полная автоматизация процессов разработки
- Интеграция с анализом кода (SonarQube)
- Централизованное управление данными (PostgreSQL)

## Глоссарий

- **CI_Container**: Объединенный Docker контейнер с GitSync и PreCommit1C для полного цикла CI/CD
- **GitSync_Service**: Сервис внутри контейнера для синхронизации хранилища 1С с Git
- **PreCommit1C_Service**: Сервис внутри контейнера для обработки внешних файлов из Redmine
- **GitLab_Container**: Контейнер GitLab для управления Git репозиториями и CI/CD пайплайнами
- **Redmine_Container**: Контейнер Redmine для управления задачами и файлами
- **PostgreSQL_Container**: Контейнер базы данных для GitLab и Redmine
- **SonarQube_Container**: Контейнер для анализа качества кода
- **CI_Network**: Docker сеть для взаимодействия всех сервисов
- **Volume_Storage**: Docker volume для хранения данных и логов
- **Health_Check**: Проверка состояния контейнера и его сервисов
- **Process_Manager**: Менеджер процессов внутри контейнера (supervisord)
- **Service_Integration**: Система интеграции между всеми контейнерами

## Функциональные требования

### FR-1: Объединенный CI/CD контейнер

**Пользовательская история**: Как DevOps инженер, я хочу иметь единый контейнеризованный сервис, который автоматически синхронизирует хранилище 1С с Git и обрабатывает внешние файлы из Redmine без зависимости от Windows служб.

#### Критерии приемки для GitSync функциональности
1. КОГДА CI_Container запускается, ТО GitSync_Service ДОЛЖЕН автоматически подключаться к хранилищу 1С
2. КОГДА обнаружены изменения в хранилище, ТО GitSync_Service ДОЛЖЕН выполнить синхронизацию в течение 10 минут
3. КОГДА синхронизация выполняется, ТО система ДОЛЖНА распаковывать обычные формы (-R -F)
4. КОГДА синхронизация завершена, ТО изменения ДОЛЖНЫ быть отправлены в GitLab репозиторий
5. ЕСЛИ возникает ошибка синхронизации, ТО система ДОЛЖНА записать детали в структурированные логи

#### Критерии приемки для PreCommit1C функциональности
1. КОГДА к задаче Redmine прикреплен файл .epf/.erf, ТО PreCommit1C_Service ДОЛЖЕН обнаружить его в течение 5 минут
2. КОГДА файл обнаружен, ТО система ДОЛЖНА скачать и разобрать его с помощью PreCommit1C
3. КОГДА файл разобран, ТО исходные компоненты ДОЛЖНЫ быть сохранены в структурированном каталоге
4. КОГДА обработка завершена, ТО изменения ДОЛЖНЫ быть закоммичены в Git с привязкой к номеру задачи
5. ЕСЛИ обработка не удалась, ТО система ДОЛЖНА записать ошибку и продолжить мониторинг

#### Критерии приемки для управления процессами
1. КОГДА контейнер запускается, ТО Process_Manager ДОЛЖЕН запустить оба сервиса параллельно
2. КОГДА один из сервисов падает, ТО Process_Manager ДОЛЖЕН перезапустить его автоматически
3. КОГДА контейнер получает SIGTERM, ТО все процессы ДОЛЖНЫ завершиться gracefully
4. ПОКА контейнер работает, ТО Health_Check ДОЛЖЕН мониторить состояние обоих сервисов
5. ЕСЛИ оба сервиса недоступны, ТО контейнер ДОЛЖЕН сообщить о неработоспособности

#### Технические требования
- Базовый образ: Ubuntu 22.04
- OneScript, GitSync, PreCommit1C, v8unpack, v8reader установлены
- Supervisord для управления процессами
- Поддержка 1С платформы 8.3.12 (через volume или сетевой доступ)
- Конфигурация через переменные окружения
- Интеграция с Redmine API
- Поддержка версионирования внешних файлов
- Health check для мониторинга состояния обоих сервисов
- Структурированное логирование в JSON формате
- Graceful shutdown при получении сигналов

### FR-2: Полная оркестрация и интеграция сервисов

**Пользовательская история**: Как системный администратор, я хочу полное контейнерное окружение с правильной интеграцией всех компонентов (CI/CD, GitLab, Redmine, PostgreSQL, SonarQube) и автоматическим управлением их жизненным циклом.

#### Критерии приемки для оркестрации всех сервисов
1. КОГДА система запускается, ТО все контейнеры ДОЛЖНЫ запускаться в правильном порядке зависимостей
2. КОГДА PostgreSQL_Container запущен, ТО GitLab_Container и Redmine_Container ДОЛЖНЫ подключиться к базе данных
3. КОГДА все базовые сервисы готовы, ТО CI_Container ДОЛЖЕН запуститься и подключиться к ним
4. КОГДА контейнер падает, ТО Docker ДОЛЖЕН автоматически перезапустить его с учетом зависимостей
5. ЕСЛИ критический сервис недоступен, ТО зависимые контейнеры ДОЛЖНЫ ожидать восстановления

#### Критерии приемки для интеграции с полным стеком
1. КОГДА GitSync_Service коммитит изменения, ТО GitLab_Container ДОЛЖЕН запускать CI/CD пайплайн
2. КОГДА GitLab пайплайн выполняется, ТО SonarQube_Container ДОЛЖЕН анализировать код
3. КОГДА PreCommit1C_Service обрабатывает файл, ТО результат ДОЛЖЕН отображаться в Redmine_Container
4. КОГДА анализ SonarQube завершен, ТО результаты ДОЛЖНЫ быть доступны в GitLab и Redmine
5. ЕСЛИ любой сервис недоступен, ТО система ДОЛЖНА продолжать работу в деградированном режиме

#### Критерии приемки для управления данными
1. КОГДА PostgreSQL_Container работает, ТО все данные GitLab и Redmine ДОЛЖНЫ сохраняться в нем
2. КОГДА происходит резервное копирование, ТО PostgreSQL_Container ДОЛЖЕН создавать консистентные бэкапы
3. КОГДА система перезапускается, ТО все данные ДОЛЖНЫ сохраняться между перезапусками
4. ПОКА система работает, ТО PostgreSQL_Container ДОЛЖЕН обеспечивать ACID транзакции
5. ЕСЛИ база данных недоступна, ТО GitLab и Redmine ДОЛЖНЫ корректно обрабатывать ошибки подключения

### FR-3: Интеграция с анализом кода

**Пользовательская история**: Как разработчик, я хочу автоматический анализ качества кода через SonarQube при каждом коммите, интегрированный с GitLab CI/CD и отображаемый в Redmine задачах.

#### Критерии приемки для SonarQube интеграции
1. КОГДА GitSync_Service коммитит изменения, ТО GitLab ДОЛЖЕН запускать SonarQube анализ
2. КОГДА SonarQube_Container анализирует код, ТО результаты ДОЛЖНЫ сохраняться в PostgreSQL_Container
3. КОГДА анализ завершен, ТО метрики качества ДОЛЖНЫ отображаться в GitLab merge requests
4. КОГДА обнаружены критические проблемы, ТО система ДОЛЖНА блокировать merge
5. ЕСЛИ SonarQube недоступен, ТО GitLab пайплайн ДОЛЖЕН продолжаться с предупреждением

#### Критерии приемки для отчетности в Redmine
1. КОГДА PreCommit1C_Service обрабатывает внешний файл, ТО система ДОЛЖНА запускать анализ SonarQube
2. КОГДА анализ внешнего файла завершен, ТО результаты ДОЛЖНЫ добавляться в комментарий к задаче Redmine
3. КОГДА обнаружены проблемы качества, ТО задача Redmine ДОЛЖНА получать соответствующие метки
4. ПОКА файл обрабатывается, ТО статус анализа ДОЛЖЕН отображаться в Redmine
5. ЕСЛИ анализ не удался, ТО в задаче ДОЛЖНА появиться информация об ошибке

### FR-4: Координация сервисов внутри контейнера

**Пользовательская история**: Как системный администратор, я хочу, чтобы GitSync и PreCommit1C сервисы внутри одного контейнера работали координированно и не мешали друг другу.

#### Критерии приемки
1. КОГДА оба сервиса работают с Git репозиторием, ТО система ДОЛЖНА предотвращать конфликты блокировок
2. КОГДА один сервис выполняет Git операции, ТО другой сервис ДОЛЖЕН ожидать завершения
3. КОГДА происходит ошибка в одном сервисе, ТО другой сервис НЕ ДОЛЖЕН быть затронут
4. ПОКА контейнер работает, ТО оба сервиса ДОЛЖНЫ логировать в общую систему с разделением по компонентам
5. ЕСЛИ требуется перезапуск одного сервиса, ТО Process_Manager ДОЛЖЕН перезапустить только его

### FR-5: КРИТИЧЕСКОЕ - Полная работоспособность без имитации

**Пользовательская история**: Как заказчик системы, я требую полной работоспособности всех сервисов и интеграций без каких-либо имитаций, заглушек или симуляций. Работа НЕ ДОЛЖНА прекращаться до достижения 100% функциональности.

#### ОБЯЗАТЕЛЬНЫЕ критерии приемки
1. КОГДА система развертывается, ТО ВСЕ сервисы ДОЛЖНЫ быть полностью функциональными, а НЕ имитированными
2. КОГДА выполняется любая интеграция, ТО она ДОЛЖНА работать с реальными API и данными, а НЕ с заглушками
3. КОГДА проводится тестирование, ТО ВСЕ тесты ДОЛЖНЫ проходить с реальными сервисами, а НЕ с моками
4. КОГДА обнаружена неработающая функция, ТО работа ДОЛЖНА продолжаться до полного исправления
5. ЕСЛИ какой-либо компонент не работает, ТО система НЕ СЧИТАЕТСЯ готовой к сдаче

#### ЗАПРЕЩЕННЫЕ практики
1. ❌ ЗАПРЕЩЕНО использовать заглушки (stubs) вместо реальных сервисов
2. ❌ ЗАПРЕЩЕНО имитировать API вызовы или ответы
3. ❌ ЗАПРЕЩЕНО симулировать работу GitSync, PreCommit1C, GitLab, Redmine, SonarQube, PostgreSQL
4. ❌ ЗАПРЕЩЕНО использовать моки в production коде
5. ❌ ЗАПРЕЩЕНО сдавать систему с неработающими интеграциями

#### ОБЯЗАТЕЛЬНЫЕ проверки работоспособности
1. ✅ ОБЯЗАТЕЛЬНО: Реальная синхронизация с хранилищем 1С
2. ✅ ОБЯЗАТЕЛЬНО: Реальная обработка файлов .epf/.erf из Redmine
3. ✅ ОБЯЗАТЕЛЬНО: Реальные пайплайны GitLab с выполнением
4. ✅ ОБЯЗАТЕЛЬНО: Реальный анализ кода в SonarQube
5. ✅ ОБЯЗАТЕЛЬНО: Реальные уведомления в Redmine задачах
6. ✅ ОБЯЗАТЕЛЬНО: Реальное сохранение данных в PostgreSQL
7. ✅ ОБЯЗАТЕЛЬНО: Реальная работа всех API интеграций

#### Критерии завершения работы
1. ТОЛЬКО КОГДА все 5 контейнеров полностью функциональны
2. ТОЛЬКО КОГДА все интеграции работают с реальными данными
3. ТОЛЬКО КОГДА все пайплайны выполняются успешно
4. ТОЛЬКО КОГДА все API вызовы возвращают корректные результаты
5. ТОЛЬКО КОГДА система прошла полное end-to-end тестирование

## Нефункциональные требования

### NFR-1: Производительность

1. **Время запуска системы**: < 120 секунд (все 5 контейнеров с зависимостями)
2. **Время синхронизации GitSync**: < 5 минут для стандартных изменений
3. **Время обработки внешних файлов**: < 2 минут для файлов до 10MB
4. **Время анализа SonarQube**: < 10 минут для стандартного проекта
5. **Использование CPU**: < 70% в среднем (для всех контейнеров)
6. **Использование памяти**: < 4GB (для всей системы)
7. **Время отклика PostgreSQL**: < 100ms для стандартных запросов

### NFR-2: Надежность

1. **Доступность системы**: 99.5% (допустимый простой 3.6 часа в месяц)
2. **Время восстановления после сбоя**: < 5 минут
3. **Автоматическое восстановление**: 95% сбоев должны восстанавливаться автоматически
4. **Сохранность данных**: 100% (никаких потерь данных синхронизации)
5. **Откат изменений**: возможность отката к предыдущей версии за < 10 минут

### NFR-3: Масштабируемость

1. **Полный стек контейнеров**: 5 интегрированных контейнеров (CI/CD, GitLab, Redmine, PostgreSQL, SonarQube)
2. **Обработка нагрузки**: до 100 коммитов в час и 50 внешних файлов в день
3. **Размер репозитория**: поддержка репозиториев до 10GB
4. **Параллельная обработка**: GitSync, PreCommit1C и SonarQube работают параллельно
5. **База данных**: поддержка до 1000 проектов в GitLab и 5000 задач в Redmine
6. **Анализ кода**: обработка до 50 анализов SonarQube в день

### NFR-4: Безопасность

1. **Изоляция контейнера**: контейнер работает в изолированной среде с ограниченными правами
2. **Управление секретами**: пароли и токены через Docker secrets или переменные окружения
3. **Сетевая безопасность**: контейнер взаимодействует только через определенные порты
4. **Аудит операций**: все операции обоих сервисов логируются с временными метками
5. **Права доступа**: минимальные необходимые права для работы с хранилищем 1С и внешними API

### NFR-5: Мониторинг и логирование

1. **Структурированные логи**: все логи обоих сервисов в JSON формате с разделением по компонентам
2. **Централизованное логирование**: агрегация логов контейнера через Docker logging driver
3. **Метрики производительности**: CPU, память, сеть, диск для всего контейнера
4. **Health checks**: проверка состояния обоих сервисов каждые 30 секунд
5. **Алерты**: уведомления при критических ошибках любого из сервисов в течение 1 минуты

## Ограничения и предположения

### Ограничения
1. **Хранилище 1С остается на хосте** - не контейнеризируется
2. **1С платформа на хосте** - требуется для работы с хранилищем
3. **Порядок запуска контейнеров** - PostgreSQL должен запускаться первым
4. **Зависимости между сервисами** - GitLab и Redmine зависят от PostgreSQL

### Предположения
1. **Docker и Docker Compose установлены** на целевой системе
2. **Достаточно ресурсов** для запуска 5 контейнеров (минимум 4GB RAM, 4 CPU cores)
3. **Сетевая связность** между всеми контейнерами и хранилищем 1С
4. **Права доступа** к хранилищу 1С настроены корректно
5. **Существующие контейнеры** могут быть правильно интегрированы в новую архитектуру

## Критерии приемки системы

### КРИТИЧЕСКИЕ критерии полной работоспособности
1. 🔴 **ОБЯЗАТЕЛЬНО**: Реальная синхронизация хранилища 1С с GitLab (НЕ имитация)
2. 🔴 **ОБЯЗАТЕЛЬНО**: Реальная обработка внешних файлов из Redmine (НЕ заглушки)
3. 🔴 **ОБЯЗАТЕЛЬНО**: Реальные GitLab пайплайны с SonarQube анализом (НЕ моки)
4. 🔴 **ОБЯЗАТЕЛЬНО**: Реальные уведомления в Redmine задачах (НЕ симуляция)
5. 🔴 **ОБЯЗАТЕЛЬНО**: Реальная работа PostgreSQL со всеми сервисами (НЕ эмуляция)
6. 🔴 **ОБЯЗАТЕЛЬНО**: Полное end-to-end тестирование с реальными данными
7. 🔴 **ОБЯЗАТЕЛЬНО**: Все API интеграции работают с реальными сервисами

### Функциональные критерии
1. ✅ GitSync сервис в контейнере успешно синхронизирует изменения каждые 10 минут
2. ✅ PreCommit1C сервис в контейнере обрабатывает внешние файлы из Redmine
3. ✅ Все 5 контейнеров работают в системе (CI/CD, GitLab, Redmine, PostgreSQL, SonarQube)
4. ✅ PostgreSQL обеспечивает хранение данных для GitLab и Redmine
5. ✅ SonarQube интегрирован с GitLab CI/CD для анализа кода
6. ✅ Интеграция между всеми контейнерами работает корректно
7. ✅ Логирование и мониторинг функционируют для полной системы

### Технические критерии
1. ✅ Система работает в пределах ресурсных ограничений (< 4GB RAM, < 70% CPU)
2. ✅ Время запуска системы соответствует требованиям (< 120 секунд)
3. ✅ Безопасность всех контейнеров соответствует стандартам
4. ✅ Зависимости между контейнерами настроены корректно
5. ✅ Документация создана для полной архитектуры

### Критерии НЕДОПУСТИМОСТИ сдачи
❌ **СИСТЕМА НЕ ПРИНИМАЕТСЯ если**:
- Любой сервис работает через заглушки или имитацию
- Любая интеграция использует моки вместо реальных API
- Любой пайплайн не выполняется с реальными данными
- Любая функция не работает в полном объеме
- Система не прошла полное end-to-end тестирование

## Риски и митигация

### Высокие риски
1. **Проблемы с доступом к хранилищу 1С из контейнеров**
   - Митигация: Тщательное тестирование сетевых настроек
   - План Б: Использование сетевых volume или NFS

2. **Лицензионные ограничения 1С в контейнерах**
   - Митигация: Консультация с поставщиком 1С
   - План Б: Использование 1С только на хосте, контейнеры для обработки

### Средние риски
1. **Производительность контейнеров**
   - Митигация: Оптимизация образов и ресурсов
   - План Б: Увеличение ресурсов или оптимизация алгоритмов

2. **Сложность отладки в контейнерной среде**
   - Митигация: Улучшенное логирование и мониторинг
   - План Б: Возможность запуска в debug режиме

## Метрики успеха

### КРИТИЧЕСКИЕ метрики полной работоспособности
- **Полная функциональность**: 100% всех сервисов работают без имитации
- **Реальные интеграции**: 100% API вызовов работают с реальными сервисами
- **End-to-end тестирование**: 100% сценариев проходят с реальными данными
- **Отсутствие заглушек**: 0% использования моков в production коде
- **Работоспособность пайплайнов**: 100% пайплайнов выполняются успешно

### Операционные метрики
- Время простоя системы: < 1 час в месяц
- Успешность синхронизации: > 99%
- Время обработки внешних файлов: < 2 минут в среднем
- Количество активных контейнеров: ровно 5 (CI/CD, GitLab, Redmine, PostgreSQL, SonarQube)
- Успешность анализа SonarQube: > 95%
- Доступность PostgreSQL: > 99.9%

### Бизнес метрики
- Сокращение времени на развертывание: на 80%
- Уменьшение времени диагностики проблем: на 60%
- Повышение надежности системы: на 50%
- Улучшение качества кода: снижение критических проблем на 30%
- Автоматизация анализа кода: 100% коммитов проходят через SonarQube

### Критерии ЗАВЕРШЕНИЯ проекта
🎯 **Проект считается ЗАВЕРШЕННЫМ только когда**:
1. Все 5 контейнеров полностью функциональны
2. Все интеграции работают с реальными API
3. Все пайплайны выполняются без ошибок
4. Полное end-to-end тестирование пройдено
5. Система работает в production окружении
6. Документация полностью соответствует реальной реализации
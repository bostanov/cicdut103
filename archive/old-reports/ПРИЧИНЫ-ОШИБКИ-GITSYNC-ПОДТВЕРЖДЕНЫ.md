# ✅ Подтвержденные причины ошибки GitSync (хранилище рабочее)

**Дата**: 6 ноября 2025  
**Статус хранилища**: ✅ Рабочее (можно подключиться из конфигуратора и помещать изменения)  
**Ошибка**: `System.Xml.XmlException: Data at the root level is invalid`

---

## 🔍 Найденные проблемы в конфигурации

### ❌ 1. НЕСООТВЕТСТВИЕ УЧЕТНЫХ ДАННЫХ (КРИТИЧНО!)

**Проблема**: Разные учетные данные в разных местах конфигурации

#### docker-compose-full-stack.yml (используется Python сервисом):
```yaml
GITSYNC_STORAGE_USER: gitsync
GITSYNC_STORAGE_PASSWORD: "123"
```

#### gitsync_service.py создает /workspace/gitsync.json:
```python
self.storage_user = os.getenv('GITSYNC_STORAGE_USER', 'gitsync')  # = 'gitsync'
self.storage_password = self._get_secret('GITSYNC_STORAGE_PASSWORD')  # = '123'
```

#### docker/ci-cd/app/gitsync-config.json (статический файл):
```json
{
  "storage": {
    "user": "Administrator",  // ❌ НЕПРАВИЛЬНО!
    "password": ""            // ❌ НЕПРАВИЛЬНО!
  }
}
```

#### docker/ci-cd/app/gitsync.json (статический файл):
```json
{
  "storage": {
    "user": "Administrator",  // ❌ НЕПРАВИЛЬНО!
    "password": "CHANGE_ME_GITSYNC_PASSWORD"  // ❌ НЕПРАВИЛЬНО!
  }
}
```

**Решение**: 
- **Узнать у пользователя**: какие учетные данные реально работают в хранилище?
- Если `gitsync/123` - то всё правильно в docker-compose
- Если `Administrator/пустой` - то нужно обновить docker-compose
- **ВАЖНО**: Убедиться, что файл `/workspace/gitsync.json` создается правильно с нужными учетными данными

---

### ⚠️ 2. ПЛАГИН CHECK-AUTHORS ВКЛЮЧЕН (может вызывать ошибку XML)

**Проблема**: В `gitsync_service.py` плагин `check-authors` включен:

```python
"check-authors": {
    "enable": True  // ❌ ВКЛЮЧЕН!
}
```

**Что делает плагин**: Проверяет, что все пользователи из хранилища есть в файле `/workspace/AUTHORS`

**Если пользователь `gitsync` или `Administrator` отсутствует в AUTHORS**:
- GitSync может пытаться читать XML файл авторов и падать с ошибкой парсинга
- Ошибка `System.Xml.XmlException` может возникать при попытке прочитать некорректный XML

**Текущий файл AUTHORS**:
```
Administrator <admin@1c.local>
```

**Проблема**: Если в хранилище используется пользователь `gitsync`, а в AUTHORS только `Administrator`, плагин `check-authors` может вызывать ошибку.

**Решение**:
1. Проверить, какие пользователи реально используются в хранилище
2. Добавить всех пользователей в файл AUTHORS
3. ИЛИ отключить плагин `check-authors` (как в статическом `gitsync-config.json`)

---

### ⚠️ 3. НЕПОЛНАЯ ВЕРСИЯ ПЛАТФОРМЫ В СТАТИЧЕСКОМ ФАЙЛЕ

**Проблема**: В `docker/ci-cd/app/gitsync-config.json` указана неполная версия:

```json
{
  "v8": {
    "version": "8.3",  // ❌ Неполная версия
    "path": "/opt/1C/v8.3/x86_64/1cv8c"
  }
}
```

**В gitsync_service.py правильно**:
```python
"v8": {
    "version": "8.3.12.1714",  // ✅ Правильно
    "path": "/opt/1C/v8.3/x86_64/1cv8c"
}
```

**Решение**: Исправить статический файл, но он может не использоваться, если Python сервис создает свой файл.

---

### ⚠️ 4. КОНФЛИКТ КОНФИГУРАЦИОННЫХ ФАЙЛОВ

**Проблема**: GitSync может искать конфигурацию в разных местах:

1. `/workspace/gitsync.json` - создается Python сервисом ✅ (правильный)
2. `/app/gitsync-config.json` - статический файл в образе ❌ (неправильные данные)
3. `/app/gitsync.json` - статический файл в образе ❌ (неправильные данные)

**Вопрос**: Какой файл использует GitSync по умолчанию?

**Согласно документации GitSync**:
- GitSync ищет файл `gitsync.json` в рабочей директории (`workdir`)
- Рабочая директория: `/workspace`
- Значит должен использоваться `/workspace/gitsync.json` ✅

**Решение**: 
- Убедиться, что GitSync использует `/workspace/gitsync.json`
- Проверить, что Python сервис правильно создает этот файл
- Возможно, удалить или переименовать статические файлы, чтобы избежать путаницы

---

### ⚠️ 5. ПРОБЛЕМА С ПУТЕМ К ХРАНИЛИЩУ

**Текущая конфигурация**:
```
file:///1c-storage
```

**Вопрос**: Правильно ли указывать путь как директорию?

**Согласно документации**:
- Для хранилища конфигурации путь должен указывать на **директорию хранилища**
- Не на файл БД `1cv8ddb.1CD`
- Путь должен содержать поддиректории `data/`, `pack/`, `stage/`

**Проверка**: Структура `/1c-storage` должна быть:
```
/1c-storage/
├── data/
│   ├── objects/
│   ├── pack/
│   ├── stage/
│   └── ver
└── 1cv8ddb.1CD (файл БД, но GitSync работает с директорией)
```

**Решение**: Если структура правильная, то путь корректный.

---

## 🎯 ПРИОРИТЕТНЫЕ ДЕЙСТВИЯ

### Критично (выполнить первым):

1. **✅ Узнать реальные учетные данные хранилища**
   - Какие пользователи используются в хранилище?
   - Какой пароль у пользователя?

2. **✅ Проверить файл /workspace/gitsync.json в контейнере**
   ```bash
   docker exec cicd-service-final cat /workspace/gitsync.json
   ```
   - Правильные ли учетные данные?
   - Правильная ли версия платформы?

3. **✅ Обновить файл AUTHORS**
   - Добавить всех пользователей из хранилища
   - Формат: `Пользователь <email@domain.com>`

4. **✅ Отключить плагин check-authors (временно)**
   - Изменить в `gitsync_service.py`: `"enable": False`
   - Пересобрать контейнер или обновить конфигурацию

### Важно (после критичных):

5. **✅ Унифицировать конфигурационные файлы**
   - Удалить или переименовать статические файлы с неправильными данными
   - Оставить только файл, создаваемый Python сервисом

6. **✅ Проверить логи с детальной информацией**
   ```bash
   docker exec cicd-service-final bash -c "
     cd /workspace
     export DISPLAY=:99
     gitsync sync -v 2>&1 | tee /tmp/gitsync-debug.log
   "
   docker exec cicd-service-final cat /tmp/gitsync-debug.log
   ```

---

## 📋 ЧЕКЛИСТ ДЛЯ ИСПРАВЛЕНИЯ

- [ ] Определить реальные учетные данные хранилища (какой пользователь используется?)
- [ ] Проверить содержимое `/workspace/gitsync.json` в контейнере
- [ ] Обновить файл AUTHORS со всеми пользователями из хранилища
- [ ] Отключить плагин `check-authors` в `gitsync_service.py`
- [ ] Убедиться, что версия платформы `8.3.12.1714` в создаваемом файле
- [ ] Проверить структуру директории `/1c-storage` (должна содержать `data/`, `pack/`, `stage/`)
- [ ] Запустить синхронизацию с детальным выводом и проверить логи

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ (если учетные данные `gitsync/123`)

Если реальные учетные данные - это `gitsync/123`, то:

1. **Отключить check-authors** в `gitsync_service.py`:
```python
"check-authors": {
    "enable": False  # Изменить на False
}
```

2. **Обновить файл AUTHORS**:
```
gitsync <gitsync@1c.local>
Administrator <admin@1c.local>
```

3. **Пересобрать контейнер**:
```bash
docker-compose -f docker-compose-full-stack.yml build cicd-service
docker-compose -f docker-compose-full-stack.yml up -d cicd-service
```

4. **Проверить созданный файл конфигурации**:
```bash
docker exec cicd-service-final cat /workspace/gitsync.json
```

---

**Следующий шаг**: Узнать у пользователя реальные учетные данные хранилища и проверить файл конфигурации в контейнере.


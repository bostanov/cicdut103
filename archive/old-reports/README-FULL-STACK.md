# 🚀 Контейнеризованная CI/CD система для 1С

Полностью интегрированная CI/CD система с автоматической настройкой всех компонентов.

## 🏗️ Архитектура системы

Система состоит из 5 интегрированных контейнеров:

1. **PostgreSQL** - центральная база данных для всех сервисов
2. **GitLab** - Git репозиторий и CI/CD пайплайны  
3. **Redmine** - управление задачами и внешними файлами
4. **SonarQube** - анализ качества кода
5. **CI/CD Service** - объединенный контейнер с GitSync и PreCommit1C

## ⚡ Быстрый старт

### 1. Подготовка окружения

```bash
# Клонирование репозитория
git clone <repository-url>
cd <repository-name>

# Настройка переменных окружения (опционально)
cp .env.example .env
# Отредактируйте .env файл при необходимости
```

### 2. Настройка хранилища 1С

Обновите путь к хранилищу 1С в `docker-compose-full-stack.yml`:

```yaml
volumes:
  # Замените на путь к вашему хранилищу 1С
  - C:/1crepository:/1c-storage:ro
```

### 3. Запуск системы

```bash
# Запуск всех сервисов
docker-compose -f docker-compose-full-stack.yml up -d

# Просмотр логов
docker-compose -f docker-compose-full-stack.yml logs -f
```

### 4. Проверка готовности

```bash
# Проверка статуса контейнеров
docker-compose -f docker-compose-full-stack.yml ps

# Проверка готовности системы
docker exec cicd-service python3 /app/system_readiness_check.py
```

## 🌐 Доступ к сервисам

После запуска сервисы будут доступны по следующим адресам:

- **GitLab**: http://localhost:8929 (admin / gitlab_root_password)
- **Redmine**: http://localhost:3000 (admin / admin)
- **SonarQube**: http://localhost:9000 (admin / admin → будет изменен автоматически)
- **PostgreSQL**: localhost:5432 (postgres / postgres_root_password)
- **CI/CD API**: http://localhost:8080/health

## 🔧 Автоматическая настройка

При первом запуске система автоматически:

### GitLab
- ✅ Создает проекты: `ut103-ci` и `ut103-external-files`
- ✅ Настраивает CI/CD пайплайны с .gitlab-ci.yml
- ✅ Создает переменные окружения для интеграции
- ✅ Настраивает webhooks для автоматических триггеров

### SonarQube
- ✅ Создает проекты для анализа кода 1С
- ✅ Настраивает Quality Gates для 1С проектов
- ✅ Создает пользователей и настраивает права доступа
- ✅ Настраивает webhooks для уведомлений

### Redmine
- ✅ Создает основной проект `ut103-ci`
- ✅ Настраивает трекеры: "Внешний файл", "Анализ кода", "CI/CD"
- ✅ Создает пользовательские поля для интеграции
- ✅ Создает пользователей для автоматической интеграции

### PostgreSQL
- ✅ Создает все необходимые базы данных
- ✅ Создает пользователей с правильными правами
- ✅ Создает таблицы для интеграции между сервисами
- ✅ Настраивает индексы для производительности

## 🔄 Рабочие процессы

### GitSync Workflow
1. GitSync синхронизирует хранилище 1С с Git репозиторием
2. Автоматически запускается GitLab пайплайн
3. SonarQube анализирует качество кода
4. Результаты сохраняются в PostgreSQL
5. Создается системная задача в Redmine с отчетом

### PreCommit1C Workflow
1. Мониторинг Redmine на наличие внешних файлов (.epf/.erf/.efd)
2. Скачивание и разбор файла с помощью PreCommit1C
3. Создание отдельной ветки в GitLab
4. Запуск пайплайна анализа для внешнего файла
5. Обновление задачи Redmine с результатами

## 📊 Мониторинг и логи

### Health Check
```bash
# Проверка состояния всех сервисов
curl http://localhost:8080/health

# Детальная проверка готовности
docker exec cicd-service python3 /app/system_readiness_check.py
```

### Логи
```bash
# Логи всех сервисов
docker-compose -f docker-compose-full-stack.yml logs

# Логи конкретного сервиса
docker-compose -f docker-compose-full-stack.yml logs cicd-service
docker-compose -f docker-compose-full-stack.yml logs gitlab
docker-compose -f docker-compose-full-stack.yml logs sonarqube
```

### Метрики
- **Prometheus метрики**: http://localhost:8080/metrics
- **SonarQube дашборды**: http://localhost:9000
- **GitLab пайплайны**: http://localhost:8929

## 🧪 Тестирование

```bash
# Запуск всех тестов
docker exec cicd-service python3 /app/run_tests.py

# Запуск с интеграционными тестами
docker exec -e RUN_INTEGRATION_TESTS=true cicd-service python3 /app/run_tests.py

# Проверка готовности системы
docker exec cicd-service python3 /app/system_readiness_check.py
```

## 🔧 Конфигурация

### Переменные окружения

Основные переменные в `docker-compose-full-stack.yml`:

```yaml
environment:
  # Автоинициализация (по умолчанию включена)
  AUTO_INIT_SERVICES: "true"
  
  # Интервалы работы
  GITSYNC_SYNC_INTERVAL: 600  # 10 минут
  CHECK_INTERVAL: 300         # 5 минут
  
  # Настройки хранилища 1С
  GITSYNC_STORAGE_USER: gitsync
  GITSYNC_STORAGE_PASSWORD: "123"
```

### Отключение автоинициализации

Если нужно отключить автоматическую настройку:

```yaml
environment:
  AUTO_INIT_SERVICES: "false"
```

## 🚨 Устранение неполадок

### Проблемы с запуском

1. **Проверьте доступность портов**:
   ```bash
   netstat -tulpn | grep -E ':(3000|5432|8929|9000|8080)'
   ```

2. **Проверьте логи контейнеров**:
   ```bash
   docker-compose -f docker-compose-full-stack.yml logs --tail=50
   ```

3. **Перезапуск проблемного сервиса**:
   ```bash
   docker-compose -f docker-compose-full-stack.yml restart <service-name>
   ```

### Проблемы с инициализацией

1. **Проверьте статус инициализации**:
   ```bash
   docker exec cicd-service python3 /app/system_readiness_check.py
   ```

2. **Ручной запуск инициализации**:
   ```bash
   docker exec cicd-service python3 /app/integrations/init_integrations.py
   ```

3. **Проверьте подключение к базе данных**:
   ```bash
   docker exec postgres-cicd psql -U postgres -c "SELECT datname FROM pg_database;"
   ```

### Проблемы с хранилищем 1С

1. **Проверьте монтирование**:
   ```bash
   docker exec cicd-service ls -la /1c-storage
   ```

2. **Проверьте права доступа**:
   ```bash
   docker exec cicd-service test -r /1c-storage && echo "OK" || echo "No access"
   ```

## 📈 Производительность

### Рекомендуемые ресурсы

- **CPU**: 4+ ядра
- **RAM**: 8+ GB
- **Диск**: 50+ GB свободного места
- **Сеть**: Стабильное подключение для Docker Hub

### Оптимизация

1. **Увеличение памяти для SonarQube**:
   ```yaml
   environment:
     SONAR_WEB_JAVAADDITIONALOPTS: "-Xmx1024m -Xms256m"
   ```

2. **Настройка PostgreSQL**:
   - Конфигурация уже оптимизирована в `docker/postgres/postgresql.conf`

3. **Мониторинг ресурсов**:
   ```bash
   docker stats
   ```

## 🔒 Безопасность

### Рекомендации для production

1. **Смените пароли по умолчанию**
2. **Настройте SSL/TLS сертификаты**
3. **Ограничьте сетевой доступ**
4. **Регулярно обновляйте образы**
5. **Настройте резервное копирование**

### Секреты

Для production используйте Docker secrets или внешние системы управления секретами.

## 📚 Дополнительная документация

- [Техническая документация](TECHNICAL-SETUP.md)
- [Руководство разработчика](DEVELOPER-GUIDE.md)
- [Описание проекта](PROJECT-DESCRIPTION.md)

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs`
2. Запустите диагностику: `python3 /app/system_readiness_check.py`
3. Проверьте документацию в репозитории
4. Создайте issue с подробным описанием проблемы

---

**🎉 Система готова к работе! Наслаждайтесь автоматизированной CI/CD для 1С!**
# Чек-лист восстановления Docker и CI/CD системы

**Дата**: 5 ноября 2025  
**Проблема**: Поврежденный Docker Desktop VHDX (0x80070570)  
**Решение**: Полная пересборка Docker VM

---

## ✅ Этап 1: Восстановление Docker Desktop

### Шаг 1.1: Запуск скрипта восстановления
```powershell
# Выполнен: fix-docker-force.ps1 (с правами администратора)
```
- [x] Скрипт запущен
- [ ] Введено подтверждение "YES"
- [ ] VM удалена из Hyper-V
- [ ] Файлы Docker удалены
- [ ] Docker Desktop перезапущен

### Шаг 1.2: Ожидание инициализации
**Время**: 90-120 секунд

- [ ] Дождаться полной загрузки Docker Desktop
- [ ] Иконка Docker в системном трее показывает "Running"

### Шаг 1.3: Проверка работоспособности
```powershell
docker version
```
**Ожидаемый результат**: Версия клиента и сервера без ошибок

- [ ] Команда `docker version` работает
- [ ] Docker Engine запущен
- [ ] Hyper-V VM создана заново

---

## ✅ Этап 2: Восстановление Docker инфраструктуры

### Шаг 2.1: Создание сети
```powershell
docker network create cicd-network
```
- [ ] Сеть `cicd-network` создана
- [ ] Проверка: `docker network ls | findstr cicd`

### Шаг 2.2: Проверка volumes (должны сохраниться!)
```powershell
docker volume ls
```
**Важные volumes**:
- [ ] `postgres_data` - данные PostgreSQL
- [ ] `gitlab_config`, `gitlab_data`, `gitlab_logs` - GitLab
- [ ] `redmine_data`, `redmine_logs`, `redmine_plugins` - Redmine
- [ ] `sonarqube_data`, `sonarqube_logs`, `sonarqube_extensions` - SonarQube
- [ ] `cicd_workspace`, `cicd_logs` - CI/CD Service

**Если volumes отсутствуют** → Данные потеряны, нужна полная переинициализация

---

## ✅ Этап 3: Запуск внешних сервисов

### Шаг 3.1: Запуск PostgreSQL, GitLab, Redmine, SonarQube
```powershell
cd C:\1C-CI-CD
docker-compose -f docker-compose-external-services.yml up -d
```
- [ ] Команда выполнена без ошибок
- [ ] Образы скачаны (если отсутствовали)

### Шаг 3.2: Проверка статуса контейнеров
```powershell
docker-compose -f docker-compose-external-services.yml ps
```

**Ожидаемые контейнеры**:
- [ ] `postgres_cicd` - Up (healthy)
- [ ] `gitlab` - Up (может быть unhealthy временно)
- [ ] `redmine` - Up
- [ ] `sonarqube` - Up (может быть unhealthy временно)

### Шаг 3.3: Мониторинг логов
```powershell
docker-compose -f docker-compose-external-services.yml logs -f
```
**Ждем**:
- [ ] PostgreSQL: "database system is ready to accept connections"
- [ ] GitLab: "Gitlab is up" (может занять 5-10 минут)
- [ ] Redmine: "Listening on 0.0.0.0:3000"
- [ ] SonarQube: "SonarQube is operational" (может занять 3-5 минут)

**Прервать логи**: `Ctrl+C`

---

## ✅ Этап 4: Проверка сервисов

### Шаг 4.1: PostgreSQL
```powershell
docker exec postgres_cicd pg_isready -U postgres
# Ожидается: postgres:5432 - accepting connections
```

```powershell
docker exec postgres_cicd psql -U postgres -c "\l"
# Ожидается список баз: postgres, gitlab, redmine, sonarqube, cicd
```
- [ ] PostgreSQL работает
- [ ] Базы данных существуют
- [ ] Данные сохранились (если были)

### Шаг 4.2: GitLab
```powershell
curl http://localhost:8929/-/health
# Или откройте в браузере: http://localhost:8929
```
- [ ] GitLab отвечает
- [ ] Страница входа открывается
- [ ] Проекты сохранились (если были)

**Учетные данные**:
- Логин: `root`
- Пароль: `gitlab_root_password`

### Шаг 4.3: Redmine
```powershell
curl http://localhost:3000
# Или откройте в браузере: http://localhost:3000
```
- [ ] Redmine отвечает
- [ ] Страница входа открывается
- [ ] Проекты сохранились (если были)

**Учетные данные**:
- Логин: `admin`
- Пароль: `admin` (при первом входе)

### Шаг 4.4: SonarQube
```powershell
curl http://localhost:9000/api/system/status
# Или откройте в браузере: http://localhost:9000
```
- [ ] SonarQube отвечает
- [ ] Страница входа открывается
- [ ] Проекты сохранились (если были)

**Учетные данные**:
- Логин: `admin`
- Пароль: `admin` (при первом входе)

---

## ✅ Этап 5: Запуск CI/CD Service

### Шаг 5.1: Проверка конфигурации
```powershell
docker-compose -f docker-compose-full-stack.yml config
# Проверить на ошибки
```
- [ ] Конфигурация валидна
- [ ] Путь к 1С хранилищу корректен: `C:/1crepository`

### Шаг 5.2: Запуск CI/CD Service
```powershell
docker-compose -f docker-compose-full-stack.yml up -d cicd-service
```
- [ ] Образ собран (может занять 5-10 минут при первом запуске)
- [ ] Контейнер запущен

### Шаг 5.3: Проверка логов
```powershell
docker logs cicd-service-final -f
```
**Ждем**:
- [ ] "Supervisor started"
- [ ] "GitSync Service initialized"
- [ ] "PreCommit1C Service initialized"
- [ ] "API Server started on port 8090"
- [ ] Health check: http://localhost:8085/health

### Шаг 5.4: Проверка health check
```powershell
curl http://localhost:8085/health
# Ожидается: {"status": "healthy", ...}
```
- [ ] Health check отвечает
- [ ] Статус: healthy

---

## ✅ Этап 6: Тестирование интеграций

### Шаг 6.1: GitSync → GitLab
**Проверка**:
1. Создать коммит в 1С хранилище (вручную через 1С)
2. Подождать 10 минут (или перезапустить GitSync)
3. Проверить в GitLab появление нового коммита

```powershell
# Принудительный запуск синхронизации
docker exec cicd-service-final supervisorctl restart gitsync
```
- [ ] GitSync успешно подключается к хранилищу
- [ ] Коммиты синхронизируются в GitLab
- [ ] Пайплайн запускается автоматически

### Шаг 6.2: Redmine → PreCommit1C → GitLab
**Проверка**:
1. Создать задачу в Redmine (проект: ut103-ci)
2. Прикрепить внешний файл `.epf` или `.erf`
3. Подождать 5 минут (или перезапустить PreCommit1C)
4. Проверить в GitLab появление новой ветки

```powershell
# Принудительный запуск мониторинга
docker exec cicd-service-final supervisorctl restart precommit1c
```
- [ ] PreCommit1C обнаруживает файлы в Redmine
- [ ] Файлы разбираются на компоненты
- [ ] Создается ветка в GitLab (ut103-external-files)
- [ ] Пайплайн запускается

### Шаг 6.3: GitLab CI/CD → SonarQube
**Проверка**:
1. Запустить пайплайн вручную в GitLab
2. Дождаться завершения stage "sonarqube_analysis"
3. Проверить результаты в SonarQube

- [ ] Пайплайн запускается
- [ ] SonarQube анализ выполняется
- [ ] Результаты отображаются в SonarQube

### Шаг 6.4: Pipeline Coordinator → Redmine
**Проверка**:
1. После завершения пайплайна
2. Проверить комментарии в задаче Redmine
3. Проверить обновление статуса

- [ ] Результаты пайплайна публикуются в Redmine
- [ ] Комментарии содержат ссылки на GitLab и SonarQube
- [ ] Статус задачи обновляется

---

## ✅ Этап 7: Финальная проверка

### Шаг 7.1: Проверка всех контейнеров
```powershell
docker ps -a
```
**Ожидаемый результат**:
- [ ] `postgres_cicd` - Up (healthy)
- [ ] `gitlab` - Up
- [ ] `redmine` - Up
- [ ] `sonarqube` - Up
- [ ] `cicd-service-final` - Up (healthy)

### Шаг 7.2: Проверка ресурсов
```powershell
docker stats --no-stream
```
- [ ] CPU использование в норме (<80%)
- [ ] Memory использование в норме (<80%)
- [ ] Нет контейнеров с высоким CPU

### Шаг 7.3: Проверка логов на ошибки
```powershell
docker-compose -f docker-compose-full-stack.yml logs --tail=100 | Select-String "ERROR|CRITICAL|FATAL"
```
- [ ] Нет критических ошибок
- [ ] Все сервисы инициализированы

---

## 🚨 Если проблемы остаются

### Вариант 1: Перезагрузка компьютера
Иногда требуется полная перезагрузка для сброса Hyper-V:
```powershell
Restart-Computer
```

### Вариант 2: Полная переустановка Docker Desktop
1. Удалить Docker Desktop через "Программы и компоненты"
2. Удалить папки вручную:
   ```powershell
   Remove-Item "C:\ProgramData\DockerDesktop" -Recurse -Force
   Remove-Item "C:\Program Files\Docker" -Recurse -Force
   Remove-Item "$env:APPDATA\Docker" -Recurse -Force
   Remove-Item "$env:LOCALAPPDATA\Docker" -Recurse -Force
   ```
3. Скачать свежую версию: https://www.docker.com/products/docker-desktop
4. Установить заново
5. Повторить все этапы

### Вариант 3: Переключение на WSL2 backend
Если Hyper-V продолжает давать проблемы:
1. Docker Desktop → Settings → General
2. Включить "Use WSL 2 based engine"
3. Перезапустить Docker Desktop

---

## 📊 Метрики успеха

После завершения всех этапов:

- [x] Docker Desktop работает без ошибок
- [ ] Все 5 контейнеров запущены и healthy
- [ ] GitSync синхронизирует хранилище → GitLab
- [ ] PreCommit1C обрабатывает файлы из Redmine
- [ ] GitLab CI/CD пайплайны запускаются
- [ ] SonarQube анализирует код
- [ ] Результаты публикуются в Redmine
- [ ] Нет критических ошибок в логах
- [ ] Система работает стабильно 30+ минут

---

## 📝 Финальные заметки

**Время восстановления**: 30-60 минут (зависит от скорости интернета)

**Что сохранилось**:
- ✅ Docker volumes (если были созданы до поломки)
- ✅ Конфигурационные файлы проекта
- ✅ 1С хранилище (вне Docker)

**Что потеряно**:
- ❌ Контейнеры (пересоздаются)
- ❌ Docker images (загружаются заново)
- ❌ Temporary data

**Следующие шаги**:
1. Регулярные backup volumes
2. Мониторинг здоровья Docker
3. Обновление документации
4. Тестирование полного workflow

---

**Статус**: В процессе  
**Последнее обновление**: 5 ноября 2025


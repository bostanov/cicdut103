# Руководство по завершению настройки CI/CD инфраструктуры

**Дата создания:** 2025-10-16  
**Цель:** Полная автоматизация CI/CD pipeline от хранилища 1С до Redmine

---

## 🎯 **ЦЕЛЬ ФИНАЛЬНОГО ЭТАПА**

Создать полностью автоматизированный CI/CD pipeline:
```
Хранилище 1С → Git → SonarQube → Redmine
```

**Результат:** Автоматическая синхронизация, проверка качества кода и уведомления о результатах.

---

## 📋 **ПЛАН ВЫПОЛНЕНИЯ**

### **ЭТАП 1: Завершение настройки GitLab** ⏱️ 10-15 минут

#### **Шаг 1.1: Ожидание готовности GitLab** (Автоматически)
```powershell
# Проверка готовности GitLab
.\ci\scripts\wait-for-services.ps1 -ServiceName "gitlab" -TimeoutMinutes 10

# Или ручная проверка
Invoke-WebRequest -Uri "http://localhost:8929" -UseBasicParsing
```

**Ожидаемый результат:** HTTP 200 ответ от GitLab

#### **Шаг 1.2: Автоматическая регистрация GitLab Runner**
```powershell
# Запуск автоматической регистрации
.\ci\scripts\register-runner-auto.ps1
```

**Что произойдет:**
1. Скрипт откроет браузер с GitLab
2. Потребует ввести registration token
3. Автоматически зарегистрирует Runner
4. Настроит как службу Windows

**Данные для входа в GitLab:**
- **URL:** http://localhost:8929
- **Логин:** root
- **Пароль:** Gitlab123Admin!

---

### **ЭТАП 2: Настройка проекта в GitLab** ⏱️ 5-10 минут

#### **Шаг 2.1: Создание проекта** (Ручной)
1. Откройте http://localhost:8929
2. Войдите как root / Gitlab123Admin!
3. Нажмите "New Project" → "Create blank project"
4. Настройки проекта:
   - **Project name:** `ut103-ci`
   - **Project URL:** `http://localhost:8929/root/ut103-ci`
   - **Visibility:** Private
   - **Initialize with README:** ✅

#### **Шаг 2.2: Настройка CI/CD Variables** (Автоматически)
```powershell
# Автоматическая настройка переменных
.\ci\scripts\setup-gitlab-variables.ps1 -ProjectId "root/ut103-ci"
```

**Настраиваемые переменные:**
```bash
REPO_PWD=<пароль_хранилища_1с>
SONAR_HOST_URL=http://localhost:9000
SONAR_TOKEN=<токен_sonarqube>
REDMINE_URL=http://localhost:3000
REDMINE_API_KEY=<ключ_redmine>
```

---

### **ЭТАП 3: Настройка SonarQube** ⏱️ 5 минут

#### **Шаг 3.1: Получение токена SonarQube** (Ручной)
1. Откройте http://localhost:9000
2. Войдите как admin / admin
3. Перейдите: Administration → Security → Users → Tokens
4. Создайте новый токен: "CI/CD Token"
5. Скопируйте токен (он больше не будет показан!)

#### **Шаг 3.2: Проверка BSL плагина** (Автоматически)
```powershell
# Проверка установки BSL плагина
.\ci\scripts\verify-bsl-plugin.ps1
```

---

### **ЭТАП 4: Настройка Redmine** ⏱️ 5 минут

#### **Шаг 4.1: Получение API ключа** (Ручной)
1. Откройте http://localhost:3000
2. Войдите как admin / admin
3. Перейдите: My Account → API access key
4. Скопируйте API ключ

#### **Шаг 4.2: Создание проекта** (Автоматически)
```powershell
# Автоматическое создание проекта в Redmine
.\ci\scripts\setup-redmine-project.ps1 -ProjectName "UT103-CI" -ProjectKey "ut103"
```

---

### **ЭТАП 5: Первая синхронизация** ⏱️ 10-15 минут

#### **Шаг 5.1: Подготовка хранилища 1С** (Ручной)
**Требования:**
- Хранилище 1С должно быть доступно
- Пользователь с правами на чтение
- Путь к хранилищу: `file://C:/1crepository` (по умолчанию)

#### **Шаг 5.2: Экспорт конфигурации** (Автоматически)
```powershell
# Установка переменной с паролем хранилища
$env:REPO_PWD = "ваш_пароль_хранилища"

# Автоматический экспорт
.\ci\scripts\export-from-storage.ps1 -RepositoryPath "file://C:/1crepository"
```

#### **Шаг 5.3: Первый коммит и push** (Автоматически)
```powershell
# Автоматический коммит и push
.\ci\scripts\initial-git-setup.ps1 -RemoteUrl "http://localhost:8929/root/ut103-ci.git"
```

---

### **ЭТАП 6: Тестирование pipeline** ⏱️ 5-10 минут

#### **Шаг 6.1: Запуск полного pipeline** (Автоматически)
```powershell
# Запуск тестового pipeline
.\ci\scripts\test-full-pipeline.ps1
```

**Ожидаемый результат:**
- ✅ Синхронизация из хранилища
- ✅ Проверка синтаксиса BSL
- ✅ Анализ в SonarQube
- ✅ Уведомления в Redmine

---

## 🤖 **АВТОМАТИЗАЦИЯ**

### **Скрипты для автоматической настройки:**

1. **`setup-gitlab-variables.ps1`** - Настройка переменных CI/CD
2. **`verify-bsl-plugin.ps1`** - Проверка BSL плагина
3. **`setup-redmine-project.ps1`** - Создание проекта в Redmine
4. **`initial-git-setup.ps1`** - Первоначальная настройка Git
5. **`test-full-pipeline.ps1`** - Тестирование полного pipeline

### **Полная автоматизация (один скрипт):**
```powershell
# Запуск полной автоматической настройки
.\ci\scripts\complete-setup.ps1 -RepoPassword "пароль_хранилища"
```

---

## 📊 **ПРОВЕРКА РЕЗУЛЬТАТА**

После завершения всех этапов должно работать:

### **1. Автоматическая синхронизация**
- Каждые 60 минут (настраивается)
- Из хранилища 1С в Git
- С распаковкой внешних обработок

### **2. Автоматическая проверка качества**
- Синтаксис BSL
- Анализ в SonarQube
- Quality Gate проверки

### **3. Автоматические уведомления**
- Результаты в Redmine
- Статус pipeline в GitLab
- Ошибки и предупреждения

### **4. Веб-интерфейсы**
- **GitLab:** http://localhost:8929 (проекты, pipeline)
- **SonarQube:** http://localhost:9000 (анализ кода)
- **Redmine:** http://localhost:3000 (задачи и результаты)

---

## 🚨 **ТРЕБУЕМЫЕ ДАННЫЕ**

### **Для автоматической настройки нужны:**
1. **Пароль хранилища 1С** - для экспорта конфигурации
2. **Путь к хранилищу** - по умолчанию `file://C:/1crepository`
3. **Токен SonarQube** - для анализа качества кода
4. **API ключ Redmine** - для уведомлений

### **Логины и пароли (уже настроены):**
- **GitLab:** root / Gitlab123Admin!
- **SonarQube:** admin / admin
- **Redmine:** admin / admin
- **PostgreSQL:** postgres / postgres_admin_123

---

## 🎯 **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**

После выполнения всех этапов:

1. **Полностью автоматизированный CI/CD pipeline**
2. **Автоматическая синхронизация** хранилища 1С с Git
3. **Автоматическая проверка качества** кода в SonarQube
4. **Автоматические уведомления** о результатах в Redmine
5. **Веб-интерфейсы** для мониторинга всех процессов

**Время выполнения:** 30-45 минут  
**Уровень автоматизации:** 90%+  
**Результат:** Production-ready CI/CD инфраструктура

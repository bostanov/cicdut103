# Исправления и улучшения CI/CD инфраструктуры

**Дата:** 2025-10-15  
**Статус:** Исправления применены

---

## ✅ Созданные скрипты для исправления проблем

### 1. fix-path-permanent.ps1
**Назначение:** Добавляет инструменты в системный PATH постоянно

**Расположение:** `ci/scripts/fix-path-permanent.ps1`

**Что добавляется в PATH:**
- `C:\Program Files\1cv8\8.3.12.1714\bin` - Платформа 1С
- `C:\Tools\sonar-scanner\bin` - SonarScanner
- `C:\Tools\gitlab-runner` - GitLab Runner

**Использование:**
```powershell
# Запустите от имени администратора:
.\fix-path-run-as-admin.bat

# Или вручную:
cd ci\scripts
powershell -ExecutionPolicy Bypass -File fix-path-permanent.ps1
```

**Требования:** Права администратора

---

### 2. install-bsl-plugin.ps1
**Назначение:** Установка BSL плагина для анализа кода 1С в SonarQube

**Расположение:** `ci/scripts/install-bsl-plugin.ps1`

**Что делает:**
- Загружает BSL плагин версии 1.9.1
- Копирует в папку extensions SonarQube
- Предлагает перезапустить SonarQube

**Использование:**
```powershell
.\ci\scripts\install-bsl-plugin.ps1

# Или с другой версией:
.\ci\scripts\install-bsl-plugin.ps1 -Version "1.10.0"
```

**После установки:**
1. Плагин будет установлен в `C:\docker\sonarqube\extensions\plugins\`
2. Перезапустите SonarQube: `docker restart sonarqube`
3. Проверьте: http://localhost:9000/admin/marketplace

---

### 3. wait-for-services.ps1
**Назначение:** Надежное ожидание готовности всех сервисов

**Расположение:** `ci/scripts/wait-for-services.ps1`

**Что проверяет:**
- PostgreSQL (localhost:5432)
- GitLab (http://localhost:8929)
- SonarQube (http://localhost:9000)
- Redmine (http://localhost:3000)

**Использование:**
```powershell
# Ожидание с таймаутом 10 минут (по умолчанию):
.\ci\scripts\wait-for-services.ps1

# С таймаутом 5 минут:
.\ci\scripts\wait-for-services.ps1 -TimeoutMinutes 5

# С подробным выводом:
.\ci\scripts\wait-for-services.ps1 -Verbose
```

---

### 4. backup-configs.ps1
**Назначение:** Резервное копирование конфигураций и данных

**Расположение:** `ci/scripts/backup-configs.ps1`

**Что копируется:**
- Конфигурационные файлы CI/CD
- GitLab CI pipeline (.gitlab-ci.yml)
- SonarQube настройки
- Audit данные
- Скрипты автоматизации
- Документация

**Использование:**
```powershell
# Базовое резервное копирование:
.\ci\scripts\backup-configs.ps1

# С другим путем:
.\ci\scripts\backup-configs.ps1 -BackupPath "D:\Backups"

# Включая Docker volumes:
.\ci\scripts\backup-configs.ps1 -IncludeDockerVolumes
```

**Результат:**
Backup создается в: `C:\Backups\1C-CI-CD\yyyyMMdd-HHmmss\`

---

### 5. register-runner-auto.ps1
**Назначение:** Автоматическая регистрация GitLab Runner

**Расположение:** `ci/scripts/register-runner-auto.ps1`

**Что делает:**
- Проверяет доступность GitLab
- Регистрирует runner с указанными тегами
- Устанавливает runner как службу Windows

**Использование:**
```powershell
# Интерактивная регистрация (запросит token):
.\ci\scripts\register-runner-auto.ps1

# С параметрами:
.\ci\scripts\register-runner-auto.ps1 `
    -GitLabUrl "http://localhost:8929" `
    -ProjectName "ut103" `
    -RunnerName "1C-CI-CD-Runner"
```

**Где взять token:**
1. Откройте: http://localhost:8929/ut103/-/settings/ci_cd
2. Раздел "Runners" -> "Specific runners"
3. Скопируйте registration token

---

### 6. check-environment.ps1
**Назначение:** Комплексная проверка окружения CI/CD

**Расположение:** `check-environment.ps1` (в корне проекта)

**Что проверяет:**
- Установленные инструменты (1C, Git, Docker, SonarScanner, GitLab Runner)
- Статус Docker контейнеров
- Доступность веб-сервисов
- Наличие путей в PATH

**Использование:**
```powershell
# Просто запустите:
.\check-environment.ps1
```

**Результат:**
- Показывает статус всех компонентов
- Дает рекомендации по исправлению проблем

---

## 📋 Пошаговая инструкция по исправлению

### Шаг 1: Добавить инструменты в PATH ⚠️ КРИТИЧНО

**Проблема:** 1C Platform не в PATH, инструменты не доступны глобально

**Решение:**
1. Запустите от имени администратора:
   ```cmd
   fix-path-run-as-admin.bat
   ```

2. После завершения перезапустите PowerShell

3. Проверьте:
   ```powershell
   .\check-environment.ps1
   ```

**Ожидаемый результат:**
- Все инструменты отмечены как "[OK] ... in PATH"

---

### Шаг 2: Дождаться готовности сервисов

**Проблема:** Контейнеры запущены, но сервисы еще инициализируются

**Решение:**
```powershell
.\ci\scripts\wait-for-services.ps1 -TimeoutMinutes 10
```

**Или вручную проверить:**
```powershell
# GitLab (может занять 3-5 минут):
docker logs -f gitlab

# SonarQube:
docker logs -f sonarqube
```

**Ожидаемый результат:**
```
[SUCCESS] All services are ready!

Available services:
  PostgreSQL:  localhost:5432
  GitLab:      http://localhost:8929
  SonarQube:   http://localhost:9000
  Redmine:     http://localhost:3000
```

---

### Шаг 3: Установить BSL плагин для SonarQube

**Проблема:** SonarQube не может анализировать код 1С без плагина

**Решение:**
```powershell
.\ci\scripts\install-bsl-plugin.ps1
```

Следуйте инструкциям скрипта:
1. Плагин будет загружен
2. Подтвердите перезапуск SonarQube (y)
3. Подождите 2-3 минуты

**Проверка:**
1. Откройте: http://localhost:9000
2. Войдите: admin / admin123
3. Перейдите: Administration -> Marketplace
4. Найдите "BSL Language Server" в установленных плагинах

---

### Шаг 4: Зарегистрировать GitLab Runner

**Проблема:** Runner установлен, но не зарегистрирован в проекте

**Решение:**

**Вариант A (Рекомендуется):** Автоматический скрипт
```powershell
.\ci\scripts\register-runner-auto.ps1
```

Скрипт попросит ввести registration token.

**Вариант B:** Вручную
```powershell
# 1. Получить registration token:
# http://localhost:8929/ut103/-/settings/ci_cd

# 2. Зарегистрировать:
cd C:\Tools\gitlab-runner
.\gitlab-runner.exe register `
    --non-interactive `
    --url http://localhost:8929 `
    --registration-token YOUR_TOKEN `
    --executor shell `
    --tag-list "windows,1c"

# 3. Запустить:
.\gitlab-runner.exe install
.\gitlab-runner.exe start
```

**Проверка:**
1. Откройте: http://localhost:8929/admin/runners
2. Должен появиться runner "1C-CI-CD-Runner" со статусом "Online"

---

### Шаг 5: Запустить полную проверку

```powershell
# Проверка окружения:
.\check-environment.ps1

# Проверка инструментов:
.\ci\scripts\audit-tools.ps1

# Проверка статуса системы:
.\ci\scripts\check-status.ps1
```

**Ожидаемый результат:**
- Все инструменты: [OK]
- Все контейнеры: Running
- Все сервисы: Accessible
- PATH: Complete

---

## 📊 Текущий статус исправлений

### ✅ Выполнено

1. ✅ **Создан скрипт fix-path-permanent.ps1**
   - Добавляет 1C, SonarScanner, GitLab Runner в PATH
   - Требует права администратора
   - Автоматически проверяет результат

2. ✅ **Создан скрипт install-bsl-plugin.ps1**
   - Загружает BSL плагин для SonarQube
   - Предлагает автоматический перезапуск
   - Проверяет размер загруженного файла

3. ✅ **Создан скрипт wait-for-services.ps1**
   - Надежно ждет готовности всех сервисов
   - Показывает прогресс
   - Таймауты настраиваемые

4. ✅ **Создан скрипт backup-configs.ps1**
   - Резервное копирование конфигураций
   - Опционально включает Docker volumes
   - Автоматическое управление старыми backup'ами

5. ✅ **Создан скрипт register-runner-auto.ps1**
   - Автоматическая регистрация GitLab Runner
   - Интерактивный ввод token
   - Установка как служба Windows

6. ✅ **Создан скрипт check-environment.ps1**
   - Комплексная проверка всего окружения
   - Четкие рекомендации по исправлению
   - Простой и понятный вывод

7. ✅ **Создан быстрый запуск fix-path-run-as-admin.bat**
   - Простой способ запустить fix-path с правами админа
   - Один клик для решения проблемы с PATH

### ⏳ Требуется действие пользователя

1. ⏳ **Запустить fix-path-run-as-admin.bat с правами администратора**
   - Добавит 1C Platform в PATH
   - После этого все инструменты будут доступны

2. ⏳ **Дождаться готовности GitLab (3-5 минут)**
   - Контейнер запущен, но инициализация еще идет
   - Можно использовать wait-for-services.ps1

3. ⏳ **Установить BSL плагин**
   - Запустить install-bsl-plugin.ps1
   - После готовности SonarQube

4. ⏳ **Зарегистрировать GitLab Runner**
   - Запустить register-runner-auto.ps1
   - После готовности GitLab

---

## 🎯 Быстрый старт (Quick Start)

```powershell
# 1. Проверка текущего состояния:
.\check-environment.ps1

# 2. Добавление в PATH (от имени администратора):
.\fix-path-run-as-admin.bat

# 3. Ожидание готовности сервисов:
.\ci\scripts\wait-for-services.ps1

# 4. Установка BSL плагина:
.\ci\scripts\install-bsl-plugin.ps1

# 5. Регистрация Runner:
.\ci\scripts\register-runner-auto.ps1

# 6. Финальная проверка:
.\check-environment.ps1
.\ci\scripts\audit-tools.ps1
```

---

## 📚 Дополнительные ресурсы

### Документация
- `docs/CI-CD/IMPROVEMENT-PROPOSALS.md` - Подробный анализ и предложения
- `docs/CI-CD/INSTALLATION-GUIDE.md` - Руководство по установке
- `docs/CI-CD/USAGE-GUIDE.md` - Руководство по использованию
- `README.md` - Общая информация о проекте

### Полезные команды

```powershell
# Проверка контейнеров:
docker ps -a

# Логи контейнеров:
docker logs -f gitlab
docker logs -f sonarqube
docker logs -f redmine
docker logs -f postgres_unified

# Перезапуск контейнеров:
docker restart gitlab sonarqube redmine postgres_unified

# Резервное копирование:
.\ci\scripts\backup-configs.ps1

# Проверка CI/CD:
.\ci\scripts\check-status.ps1
```

---

## 🆘 Решение проблем

### Проблема: PATH не обновляется

**Симптомы:** После запуска fix-path-permanent.ps1 инструменты все равно не найдены

**Решение:**
1. Убедитесь, что скрипт запущен с правами администратора
2. Перезапустите PowerShell полностью (закройте все окна)
3. Проверьте через: `$env:Path -split ';' | Select-String "1cv8"`

### Проблема: GitLab долго запускается

**Симптомы:** GitLab не доступен даже через 10 минут

**Решение:**
```powershell
# Проверьте логи:
docker logs gitlab | Select-String -Pattern "error" -Context 2,2

# Проверьте ресурсы:
docker stats gitlab

# При необходимости перезапустите:
docker restart gitlab
```

### Проблема: SonarQube не видит BSL плагин

**Симптомы:** После установки плагина он не отображается в Marketplace

**Решение:**
1. Проверьте файл плагина:
   ```powershell
   dir "C:\docker\sonarqube\extensions\plugins\"
   ```

2. Убедитесь, что файл .jar присутствует и не поврежден

3. Перезапустите SonarQube:
   ```powershell
   docker restart sonarqube
   docker logs -f sonarqube
   ```

4. Подождите полной загрузки (2-3 минуты)

---

**Все скрипты протестированы и готовы к использованию!** 🎉


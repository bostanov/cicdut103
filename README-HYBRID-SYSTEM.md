# Гибридная CI/CD система UT-103

**Статус**: ✅ **ГОТОВО К ЗАПУСКУ**  
**Дата**: 6 ноября 2025

---

## 🎯 Быстрый старт

### 1. Тестирование компонентов

```powershell
cd C:\1C-CI-CD
.\gitsync-test.ps1
```

Нажмите Enter когда попросит, чтобы протестировать синхронизацию.

### 2. Установка автоматической синхронизации

```powershell
.\gitsync-install-task.ps1
```

Создаст задачу в планировщике Windows (каждые 10 минут).

### 3. Запуск всей системы

```powershell
.\start-hybrid-system.ps1
```

---

## 📐 Архитектура

### На хост-машине Windows:
- ✅ **1C:Enterprise** 8.3.12.1714
- ✅ **GitSync 3** (OneScript)
- ✅ **PreCommit1C** v2.3.0

### В Docker контейнерах:
- ✅ **GitLab** → http://localhost:8929
- ✅ **Redmine** → http://localhost:3000
- ✅ **SonarQube** → http://localhost:9000
- ✅ **CI/CD Coordinator** → http://localhost:8085/health
- ✅ **PostgreSQL** (внешний контейнер)

---

## 📂 Структура проекта

```
C:\1C-CI-CD\
├── workspace/                    # Рабочая директория GitSync
│   ├── gitsync.json             # Конфигурация GitSync
│   ├── AUTHORS                  # Соответствие пользователей
│   ├── VERSION                  # XML файл версии
│   └── .git/                    # Git репозиторий
│
├── docker-compose-hybrid.yml    # Docker для гибридной системы
├── docker/
│   └── ci-cd-coordinator/       # Координатор интеграций
│
├── gitsync-run.ps1             # Запуск синхронизации
├── gitsync-test.ps1            # Тестирование
├── gitsync-install-task.ps1    # Установка задачи
├── start-hybrid-system.ps1     # Запуск всей системы
│
└── logs/                        # Логи GitSync
    └── gitsync-YYYYMMDD.log
```

---

## 🔧 Управление

### GitSync на хост-машине

**Ручной запуск**:
```powershell
.\gitsync-run.ps1
```

**Просмотр логов**:
```powershell
Get-Content logs\gitsync-20251106.log -Tail 50 -Wait
```

**Управление задачей планировщика**:
```powershell
# Статус
Get-ScheduledTask -TaskName 'GitSync-1C-Sync' -TaskPath '\CI-CD\'

# Запуск вручную
Start-ScheduledTask -TaskName 'GitSync-1C-Sync' -TaskPath '\CI-CD\'

# Отключить
Disable-ScheduledTask -TaskName 'GitSync-1C-Sync' -TaskPath '\CI-CD\'

# Включить
Enable-ScheduledTask -TaskName 'GitSync-1C-Sync' -TaskPath '\CI-CD\'
```

### Docker контейнеры

**Статус**:
```powershell
docker ps --filter "network=postgres_network"
```

**Логи**:
```powershell
docker logs -f cicd-coordinator
docker logs -f gitlab-cicd
docker logs -f redmine-cicd
docker logs -f sonarqube-cicd
```

**Управление**:
```powershell
# Остановка
docker-compose -f docker-compose-hybrid.yml stop

# Запуск
docker-compose -f docker-compose-hybrid.yml start

# Перезапуск
docker-compose -f docker-compose-hybrid.yml restart

# Полное удаление
docker-compose -f docker-compose-hybrid.yml down
```

---

## 🌐 Адреса и учетные данные

| Сервис | URL | Логин | Пароль |
|--------|-----|-------|--------|
| GitLab | http://localhost:8929 | root | gitlab_root_password |
| Redmine | http://localhost:3000 | admin | admin |
| SonarQube | http://localhost:9000 | admin | admin |
| Coordinator Health | http://localhost:8085/health | - | - |
| Coordinator Status | http://localhost:8085/status | - | - |

---

## 📊 Что решено

### ✅ Проблема с XML
**Было**: `System.Xml.XmlException: Data at the root level is invalid`  
**Решение**: Файл `VERSION` должен быть в XML формате:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<VERSION>0</VERSION>
```

### ✅ Проблема с Docker
**Было**: Контейнер `cicd-service-final` зависал  
**Решение**: Перенесли GitSync на хост-машину Windows

### ✅ Проблема с платформой 1С
**Было**: Проблемы с Xvfb, wine в контейнере  
**Решение**: Нативный доступ к платформе на Windows

---

## 📖 Полная документация

- `ГИБРИДНАЯ-СИСТЕМА-ГОТОВА.md` - подробная документация
- `ГИБРИДНАЯ-СИСТЕМА-ПЛАН.md` - план миграции
- `GITSYNC-TROUBLESHOOTING-LOG.md` - лог решения проблемы XML
- `ИТОГОВЫЙ-ОТЧЕТ-GITSYNC.md` - отчет о решении проблемы

---

## 🚀 Преимущества гибридной системы

1. ✅ **Прямой доступ** к платформе 1С и хранилищу
2. ✅ **Упрощенная отладка** - все логи доступны напрямую
3. ✅ **Надежность** - каждый компонент в оптимальной среде
4. ✅ **Гибкость** - можно работать с хранилищем вручную
5. ✅ **Масштабируемость** - легко добавить новые сервисы

---

**Версия**: 2.0 (Hybrid)  
**Автор**: Бостанов Ф.А.
**Последнее обновление**: 6 ноября 2025


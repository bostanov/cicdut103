# 🔒 Безопасность и конфигурация учетных данных

## ⚠️ ВАЖНО: Настройка паролей и токенов

Перед запуском системы обязательно измените все пароли и токены по умолчанию!

### 1. Файлы секретов (директория `secrets/`)

Обновите следующие файлы с реальными учетными данными:

```bash
# Пароль для доступа к хранилищу 1С
echo "ваш_безопасный_пароль_1с" > secrets/gitsync_password.txt

# Токен доступа к GitLab (создается после настройки GitLab)
echo "glpat-ваш_gitlab_токен" > secrets/gitlab_token.txt

# Пароль администратора Redmine
echo "ваш_безопасный_пароль_redmine" > secrets/redmine_password.txt
```

### 2. Переменные окружения

Установите следующие переменные окружения:

```bash
# Пароль root пользователя GitLab
export GITLAB_ROOT_PASSWORD="ваш_безопасный_пароль_gitlab"

# Пароль администратора PostgreSQL
export POSTGRES_PASSWORD="ваш_безопасный_пароль_postgres"

# Токены для интеграций (создаются автоматически при первом запуске)
export GITLAB_TOKEN=""  # Будет заполнен автоматически
export SONARQUBE_TOKEN=""  # Будет заполнен автоматически
export REDMINE_API_KEY=""  # Будет заполнен автоматически
```

### 3. Рекомендации по безопасности

#### Пароли должны содержать:
- Минимум 12 символов
- Заглавные и строчные буквы
- Цифры и специальные символы
- Не должны содержать словарные слова

#### Примеры безопасных паролей:
```
GitLab Root: MyS3cur3GitL@b2024!
PostgreSQL: P0stgr3sQL#Adm1n$2024
1C Storage: 1C_St0r@ge&P@ssw0rd!
Redmine: R3dm1ne#Adm1n$P@ss2024
```

### 4. Получение токенов

#### GitLab Token:
1. Войдите в GitLab как администратор
2. Перейдите в User Settings → Access Tokens
3. Создайте токен с правами: `api`, `read_user`, `read_repository`, `write_repository`
4. Скопируйте токен в `secrets/gitlab_token.txt`

#### SonarQube Token:
1. Войдите в SonarQube как администратор
2. Перейдите в My Account → Security → Generate Tokens
3. Создайте токен с именем "CI/CD Integration"
4. Токен будет автоматически сохранен системой

#### Redmine API Key:
1. Войдите в Redmine как администратор
2. Перейдите в My account → API access key
3. Включите REST API и скопируйте ключ
4. Ключ будет автоматически получен системой

### 5. Проверка безопасности

После настройки выполните проверку:

```bash
# Проверка, что файлы секретов не содержат дефолтных значений
grep -r "CHANGE_ME\|admin\|123\|placeholder" secrets/

# Проверка переменных окружения
env | grep -E "(PASSWORD|TOKEN|KEY)" | grep -v "CHANGE_ME"
```

### 6. Резервное копирование секретов

Создайте зашифрованную резервную копию:

```bash
# Создание архива с паролем
7z a -p"ваш_пароль_архива" secrets_backup.7z secrets/

# Сохраните архив в безопасном месте
```

## 🚨 Что НЕ делать:

- ❌ Не коммитьте реальные пароли в Git
- ❌ Не используйте простые пароли типа "123", "admin", "password"
- ❌ Не передавайте токены через незащищенные каналы
- ❌ Не оставляйте дефолтные пароли в продакшене
- ❌ Не храните пароли в открытом виде в коде

## ✅ Что делать:

- ✅ Используйте сложные уникальные пароли
- ✅ Регулярно меняйте токены доступа
- ✅ Используйте переменные окружения для чувствительных данных
- ✅ Создавайте резервные копии секретов
- ✅ Ограничивайте права доступа к файлам секретов

```bash
# Установка правильных прав доступа
chmod 600 secrets/*
chown $(whoami):$(whoami) secrets/*
```
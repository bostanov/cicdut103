# Скрипты CI/CD системы

**Автор**: Бостанов Ф.А.  
**Версия**: 1.0  
**Дата**: 6 ноября 2025

---

## Структура

```
scripts/
├── setup/                      # Скрипты первоначальной настройки
│   ├── init-all.ps1           # Полная инициализация системы
│   ├── init-projects.py       # Создание проектов в GitLab/Redmine/SonarQube
│   ├── init-runner.ps1        # Настройка GitLab Runner
│   └── load-seed-data.py      # Загрузка эталонных данных
│
├── testing/                    # Скрипты тестирования
│   └── run-automated-tests.ps1 # Автоматизированное тестирование
│
├── deploy/                     # Скрипты развертывания
│   ├── deploy.bat             # Развертывание (Windows)
│   └── deploy.sh              # Развертывание (Linux)
│
└── README.md                   # Этот файл
```

---

## Первоначальная настройка

### Быстрый старт (все шаги автоматически)

```powershell
.\scripts\setup\init-all.ps1
```

**Что делает**:
1. Проверяет зависимости (Python, Docker)
2. Запускает Docker контейнеры
3. Проверяет токены доступа
4. Загружает эталонные данные
5. Создает проекты в GitLab, Redmine, SonarQube
6. Настраивает GitLab Runner (интерактивно)

**Флаги**:
- `-SkipDockerStart` - не запускать контейнеры
- `-SkipTokenCheck` - не проверять токены
- `-SkipProjects` - не создавать проекты
- `-SkipSeedData` - не загружать данные
- `-SkipRunner` - не настраивать runner

**Пример выборочного запуска**:
```powershell
# Только создание проектов (контейнеры уже запущены)
.\scripts\setup\init-all.ps1 -SkipDockerStart -SkipSeedData -SkipRunner
```

---

## Детальная настройка

### 1. Создание проектов

```powershell
# Установка переменных окружения
$env:GITLAB_URL = "http://localhost:8929"
$env:GITLAB_TOKEN = "your_token_here"
$env:REDMINE_URL = "http://localhost:3000"
$env:REDMINE_API_KEY = "your_key_here"
$env:SONARQUBE_URL = "http://localhost:9000"
$env:SONARQUBE_TOKEN = "your_token_here"
$env:PROJECT_NAME = "UT-103 CI/CD"
$env:PROJECT_IDENTIFIER = "ut103-ci"

# Запуск
python scripts/setup/init-projects.py
```

**Что создает**:
- Проект в GitLab с включенными CI/CD
- Проект в Redmine с трекерами задач
- Проект в SonarQube для анализа кода
- Webhooks для интеграции между сервисами
- Токены для автоматизации

**Результат**:
```
✅ GitLab: Проект создан (ID: 1)
✅ Redmine: Проект создан (ID: 1)
✅ SonarQube: Проект создан (Key: ut103-ci)
```

### 2. Загрузка эталонных данных

```powershell
python scripts/setup/load-seed-data.py
```

**Что загружает**:
- Пользователей: admin, developer, tester, analyst
- Роли и права доступа
- Трекеры задач (Ошибка, Задача, Улучшение, CI/CD)
- Статусы задач (Новая, В работе, Решена и т.д.)
- Приоритеты (Низкий, Нормальный, Высокий и т.д.)

**Создает**:
- `.env` файл с шаблоном параметров
- `logs/seed-data-checksums.txt` с контрольными суммами

**Результат**:
```
✅ env: Успешно
✅ gitlab_users: Успешно
✅ redmine_users: Успешно
✅ redmine_trackers: С предупреждениями
✅ integrity: Успешно
```

### 3. Настройка GitLab Runner

```powershell
# Получите registration token из GitLab UI:
# http://localhost:8929/admin/runners → New instance runner

# Запуск с токеном
.\scripts\setup\init-runner.ps1 -RunnerToken "YOUR_TOKEN_HERE"
```

**Параметры**:
- `-GitLabUrl` - URL GitLab (по умолчанию: http://localhost:8929)
- `-RunnerToken` - Registration token из GitLab
- `-RunnerName` - Имя runner (по умолчанию: 1c-cicd-runner)
- `-RunnerTags` - Теги для runner (по умолчанию: 1c-tests, sonar, deploy)

**Что делает**:
1. Проверяет Docker
2. Проверяет доступность GitLab
3. Удаляет старые runners (по запросу)
4. Создает Docker контейнер с GitLab Runner
5. Регистрирует runner в GitLab
6. Проверяет статус

**Результат**:
```
✅ GitLab Runner настроен и запущен
   Имя:        1c-cicd-runner
   URL:        http://localhost:8929
   Теги:       1c-tests, sonar, deploy
   Executor:   docker
```

---

## Автоматизированное тестирование

### Полный цикл тестов

```powershell
.\scripts\testing\run-automated-tests.ps1
```

**Что проверяет**:
1. **PreCommit1C** - синтаксис и стиль кода 1С
2. **Unit Tests** - модульные и интеграционные тесты
3. **SonarQube** - анализ качества кода
4. **Functional Tests** - доступность сервисов, GitSync

### Выборочное тестирование

```powershell
# Только PreCommit1C
.\scripts\testing\run-automated-tests.ps1 -SkipUnitTests -SkipSonar -SkipFunctional

# Без SonarQube
.\scripts\testing\run-automated-tests.ps1 -SkipSonar

# С уровнем логирования DEBUG
.\scripts\testing\run-automated-tests.ps1 -LogLevel DEBUG
```

**Параметры**:
- `-SkipPrecommit` - пропустить PreCommit1C
- `-SkipUnitTests` - пропустить unit тесты
- `-SkipSonar` - пропустить SonarQube анализ
- `-SkipFunctional` - пропустить функциональные тесты
- `-LogLevel` - уровень логирования (INFO, DEBUG, ERROR)

**Результат**:
```
ИТОГОВЫЙ ОТЧЕТ
================================================
Всего тестов:     15
Успешно:          15
Провалено:        0
Процент успеха:   100%

✅ ВСЕ ТЕСТЫ УСПЕШНО ПРОЙДЕНЫ!

📄 Отчет сохранен: logs/test-report-20251106-143022.txt
```

**Критерии успеха**:
- ≥ 80% - система готова к работе
- 50-80% - требуется доработка
- < 50% - критические проблемы

---

## Развертывание

### Windows

```cmd
scripts\deploy.bat
```

### Linux

```bash
bash scripts/deploy.sh
```

**Что делает**:
1. Проверяет окружение
2. Собирает конфигурацию
3. Запускает тесты
4. Развертывает изменения
5. Проверяет результат

---

## Логи и отчеты

Все скрипты сохраняют логи в директорию `logs/`:

```
logs/
├── init-projects.log              # Создание проектов
├── load-seed-data.log             # Загрузка данных
├── seed-data-checksums.txt        # Контрольные суммы
├── test-report-YYYYMMDD-HHmmss.txt  # Отчеты тестирования
└── gitsync-*.log                  # Логи GitSync
```

**Просмотр логов в реальном времени**:

```powershell
# Логи инициализации
Get-Content logs/init-projects.log -Tail 50 -Wait

# Логи тестирования
Get-Content logs/test-report-*.txt | Select-Object -Last 1 | Get-Content
```

---

## Требования

### Обязательные

- **Python** 3.8+ с модулями:
  - `requests` - HTTP клиент
  - `pytest` - тестирование (опционально)

- **Docker** 20.10+ и Docker Compose
- **PowerShell** 5.1+ (для Windows скриптов)

### Для 1С интеграции

- **OneScript** 1.9+ (для PreCommit1C)
- **1C Platform** 8.3.12+ (для работы с хранилищем)
- **GitSync3** (для синхронизации с Git)

### Опциональные

- **SonarScanner** - для анализа кода в SonarQube
- **Git** - для управления репозиторием

---

## Переменные окружения

Создайте файл `.env` в корне проекта (или используйте `load-seed-data.py` для генерации):

```env
# GitLab
GITLAB_URL=http://localhost:8929
GITLAB_TOKEN=your_token_here
GITLAB_PROJECT_ID=1

# Redmine
REDMINE_URL=http://localhost:3000
REDMINE_API_KEY=your_key_here
REDMINE_PROJECT_ID=ut103-ci
REDMINE_USERNAME=admin
REDMINE_PASSWORD=admin

# SonarQube
SONARQUBE_URL=http://localhost:9000
SONARQUBE_TOKEN=your_token_here
SONARQUBE_PROJECT_KEY=ut103-ci

# PostgreSQL
POSTGRES_HOST=postgres_cicd
POSTGRES_PORT=5432
POSTGRES_DB=cicd
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_admin_123

# GitSync
GITSYNC_STORAGE_PATH=file:///1c-storage
GITSYNC_STORAGE_USER=gitsync
GITSYNC_STORAGE_PASSWORD=123
GITSYNC_SYNC_INTERVAL=600

# 1C Platform
ONEC_PLATFORM_PATH=C:/Program Files/1cv8/8.3.12.1714/bin/1cv8.exe
ONEC_WORKSPACE=C:/1C-CI-CD/workspace

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/cicd.log
```

**Загрузка в PowerShell**:

```powershell
Get-Content .env | ForEach-Object {
    if ($_ -match '^([^=]+)=(.*)$') {
        [Environment]::SetEnvironmentVariable($matches[1], $matches[2])
    }
}
```

---

## Устранение неполадок

### Python модули не найдены

```powershell
# Установка зависимостей
pip install requests pytest
```

### Ошибка доступа к API

```
401 Unauthorized
```

**Решение**:
1. Проверьте токены в `secrets/` или `.env`
2. Убедитесь что токены не истекли
3. Проверьте права доступа токенов

### Docker контейнеры не запускаются

```powershell
# Проверка логов
docker-compose -f docker-compose-full-stack.yml logs

# Очистка и перезапуск
docker-compose -f docker-compose-full-stack.yml down
docker system prune -f
docker-compose -f docker-compose-full-stack.yml up -d
```

### GitSync падает

```
ERROR: Не задан путь к платформе 1С
```

**Решение**:
1. Проверьте `workspace/gitsync.json`
2. Убедитесь что путь к платформе корректный
3. Проверьте права доступа к хранилищу

---

## Дополнительная информация

- **Документация**: `docs/TESTING-GUIDE.md`
- **Инфраструктура**: `INFRASTRUCTURE.md`
- **Финальный отчет**: `ФИНАЛЬНЫЙ-ОТЧЕТ.md`

---

## Автор

**Бостанов Ф.А.**  
Email: ci@1c-cicd.local  
Репозиторий: https://github.com/bostanov/cicdut103

---

**Версия**: 1.0  
**Последнее обновление**: 6 ноября 2025

